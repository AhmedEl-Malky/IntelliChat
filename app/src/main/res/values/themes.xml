<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@color/shade_white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.ChatHub" parent="Base.Theme.ChatHub" />

    <style name="chat_text">
        <item name="android:fontFamily">@font/nunito_regular</item>
        <item name="android:paddingStart">@dimen/_16sdp</item>
        <item name="android:paddingEnd">@dimen/_12sdp</item>
        <item name="android:textSize">@dimen/_16sdp</item>
        <item name="android:gravity">start</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingVertical">@dimen/_18sdp</item>
    </style>
</resources>